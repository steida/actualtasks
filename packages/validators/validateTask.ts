// import { CreateWebErrors, CreateWebInput } from '../types';
// import { validateMax140Chars } from ".";

// const validateTask = (input: CreateWebInput): CreateWebErrors => ({
//   name: validateMax140Chars(input.name),
// });

// export default validateTask;
